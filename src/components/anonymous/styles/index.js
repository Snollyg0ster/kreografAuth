import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  containerStyle: {
    borderRadius: 15,
  },
  titleStyle: {
    fontSize: 13,
    color: 'gray',
  },
});

export default styles;
